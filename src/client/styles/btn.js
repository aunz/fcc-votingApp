let base = 'py1 px2 appearance-none outline-none bold pointer '
base += ' btn-base '

const btn = `${base} white bg-color1 border-none btn `
const btnFlat = `${base} color1 bg-transparent border-none btn-flat `

export default btn
export { btnFlat }
